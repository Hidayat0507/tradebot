const ccxt = require('ccxt'); async function main() { try { const exchange = new ccxt.hyperliquid(); await exchange.loadMarkets(); const majorCoins = ['ETH', 'SOL', 'DOGE', 'XRP', 'ADA', 'AVAX']; const majorMarkets = Object.entries(exchange.markets).filter(([symbol]) => majorCoins.some(coin => symbol.includes(coin))); console.log('Major cryptocurrency markets on Hyperliquid:'); for (const [symbol, market] of majorMarkets) { console.log(`${symbol}: type=${market.type}, spot=${market.spot ? 'Yes' : 'No'}, perpetual=${market.swap ? 'Yes' : 'No'}`); } } catch (error) { console.error('Error:', error); } } main();
